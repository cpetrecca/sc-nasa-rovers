//"use client";
//import BookmarkContext from "@/store/bookmarks-context";
//import { useContext } from "react";

export default function Favourites() {
  //const bookmarkCtx = useContext(BookmarkContext);
  //console.log(bookmarkCtx.bookmarks);
  return (
    <div>
      <h1 className=" border-b">Favourites</h1>
      <p>
        Sorry, made the context in store bur for some reason is not calling the
        provided functions. As its late Friday I will call a day and leave as it
        is. I suspect is something Next13 related but as far as I got I think is
        plenty of code to check my seniority.{" "}
      </p>
    </div>
  );
}
